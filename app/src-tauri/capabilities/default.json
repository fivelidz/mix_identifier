{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "MixID default capabilities: core defaults + file/folder picker dialog.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
